<project>

    <build>
        <plugins>
            <plugin>
                <groupId>dw.maven.plugins</groupId>
                <artifactId>dw-closure-compiler-plugin</artifactId>
                <version>1.0.0</version>
                <configuration>
                    <inputDir>src/test/process</inputDir>
                    <outputDir>src/test/resources</outputDir>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>